# Allow unittest user to install/remove package
unittest    ALL = (ALL:ALL) NOPASSWD: /usr/bin/dpkg -i /mnt/postgresql-partition-manager.deb, /usr/bin/apt-get remove -y postgresql-partition-manager, /usr/bin/apt-get purge -y postgresql-partition-manager
